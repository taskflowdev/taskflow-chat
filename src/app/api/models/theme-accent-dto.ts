/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */


/**
 * DTO for theme accent color variant information
 */
export interface ThemeAccentDto {

  /**
   * Badge background color (hex format)
   */
  badgeColor?: string;

  /**
   * Error/danger color (hex format)
   */
  errorColor?: string;

  /**
   * Icon color that complements the accent (hex format)
   */
  iconColor?: string;

  /**
   * Accent ID
   */
  id?: string;

  /**
   * Whether this accent is built-in
   */
  isBuiltIn?: boolean;

  /**
   * Whether this is the default accent for the base theme
   */
  isDefault?: boolean;

  /**
   * Name of the accent variant (e.g., "Blue", "Green", "Purple")
   */
  name?: string;

  /**
   * Primary accent color for buttons, links, highlights (hex format)
   */
  primaryAccentColor?: string;

  /**
   * Secondary accent color for hover states, secondary buttons (hex format)
   */
  secondaryAccentColor?: string;

  /**
   * Success color (hex format)
   */
  successColor?: string;

  /**
   * The base theme ID this accent belongs to
   */
  themeId?: string;

  /**
   * Warning color (hex format)
   */
  warningColor?: string;
}
