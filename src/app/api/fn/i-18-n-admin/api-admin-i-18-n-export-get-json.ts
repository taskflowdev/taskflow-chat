/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface ApiAdminI18NExportGet$Json$Params {

/**
 * Language code (optional, defaults to "en")
 */
  lang?: string;

/**
 * Category filter (optional)
 */
  category?: string;

/**
 * Export format: json (default), csv, or po
 */
  format?: string;
}

export function apiAdminI18NExportGet$Json(http: HttpClient, rootUrl: string, params?: ApiAdminI18NExportGet$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<(Blob | Blob | Blob | Blob)>> {
  const rb = new RequestBuilder(rootUrl, apiAdminI18NExportGet$Json.PATH, 'get');
  if (params) {
    rb.query('lang', params.lang, {});
    rb.query('category', params.category, {});
    rb.query('format', params.format, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<(Blob | Blob | Blob | Blob)>;
    })
  );
}

apiAdminI18NExportGet$Json.PATH = '/api/admin/i18n/export';
