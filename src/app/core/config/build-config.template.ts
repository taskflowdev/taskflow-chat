/**
 * Build-time configuration constants - TEMPLATE
 * 
 * This is a template file. The actual build-config.ts is generated at build time
 * by scripts/generate-build-config.js and is git-ignored.
 * 
 * SECURITY: This file contains placeholders for sensitive values like encryption keys.
 * The actual values are set via environment variables and embedded at build time.
 * 
 * DO NOT commit the generated build-config.ts to version control.
 */

export const BUILD_CONFIG = {
  // Encryption key for localStorage (set via ENCRYPTION_KEY env var)
  ENCRYPTION_KEY: 'BUILD_TIME_ENCRYPTION_KEY_PLACEHOLDER'
};
